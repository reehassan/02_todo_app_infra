{
  "name": "todo-backend",
  "version": "1.0.0",
  "description": "Backend API for Todo Application",
  "main": "server.js",
    "author": "Areeba Hassan",
    "license": "MIT",

  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest",
    "build": "mkdir -p dist && cp -r server.js routes package.json ./dist/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "pg": "^8.11.0",
    "dotenv": "^16.0.3",
    "helmet": "^6.1.5",
    "morgan": "^1.10.0",
    "express-rate-limit": "^6.7.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22",
    "jest": "^29.5.0"
  }
}